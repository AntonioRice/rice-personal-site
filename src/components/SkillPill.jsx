const SkillPill = ({ skill }) => {
  return (
    <div className="inline-flex items-center rounded-xl border border-gray-500 bg-transparent px-4 py-2 text-center text-sm font-medium text-[#ffffff]">
      {skill}
    </div>
  );
};

export default SkillPill;
