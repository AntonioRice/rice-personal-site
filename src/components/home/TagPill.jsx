const TagPill = ({ children }) => (
  <span className="inline-block border border-rule px-[7px] py-[3px] font-mono text-[9px] uppercase tracking-[0.08em] text-dim lg:px-2 lg:py-1 lg:text-[10px]">
    {children}
  </span>
);

export default TagPill;
